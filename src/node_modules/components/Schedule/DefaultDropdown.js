import React from 'react'

const DefaultDropdown = ({ value, onChange, options }) =>
  <select value={value} onChange={event => onChange(null, event.target)}>
    {options.map(({ key, text, value }) =>
      <option value={value} key={key || value}>
        {text}
      </option>
    )}
  </select>

export default DefaultDropdown
